/**
 * feeCalculator.js — 수수료 계산 스텀 (미구현)
 *
 * 현재 모든 함수가 0을 반환한다.
 * 추후 카테고리·브랜드·사용자별 수수료 로직이 추가될 예정.
 */
function calculateFee(
  purchasePrice,
  category = null,
  brand = null,
  userId = null,
) {
  return 0;
}

function calculateFeeRate(
  purchasePrice,
  category = null,
  brand = null,
  userId = null,
) {
  return 0;
}

module.exports = {
  calculateFee,
  calculateFeeRate,
};
